apiVersion: v1
kind: Pod
metadata:
  name: hostip-vuln-pod
  namespace: legacy
spec:
  hostIPC: true
  hostNetwork: true
  hostPID: true
  containers:
  - name: vuln-container
    image: ubuntu
    command: [ "/bin/sh", "-c", "--" ]
    args: [ "while true; do sleep 30; done;" ]
